# Preview Validation

The public preview route renders the Reel Reactor home screen. The source-selection handler was updated so web picker results retain their browser-accessible URI rather than attempting to copy into the native application cache, which is unavailable in the browser preview.
